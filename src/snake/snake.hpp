#pragma once

#include <SFML/Graphics.hpp>

#include <iostream>
#include <vector>
#include <utility>

class snake {
    private:
        int len;
        std::vector<std::pair<int, int>> points_pairs;
        bool alive;
    private:


};